import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import path from "node:path";import { releaseAtLeast } from "./release-version.ts";const root=path.resolve(import.meta.dirname,".."),read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
test("no-key OpenStreetMap basemap replacement remains",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tile\.openstreetmap\.org/);assert.match(leaflet,/replaceLegacyCartoBasemap/);assert.doesNotMatch(leaflet,/api[_-]?key/i)});test("dark tile treatment remains",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tilePane\.style\.filter/);assert.match(leaflet,/invert\(/)});test("v1.31 map baseline remains supported",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,4))});
