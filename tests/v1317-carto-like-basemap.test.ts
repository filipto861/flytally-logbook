import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";const read=(p:string)=>fs.readFileSync(new URL("../"+p,import.meta.url),"utf8"),releaseAtLeast=(v:string,a:number,b:number,c:number)=>{const[x,y,z]=v.split(".").map(Number);return x>a||x===a&&(y>b||y===b&&z>=c)};
test("CARTO-like visual tuning remains over the FlyTally proxy basemap",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/invert\(\.78\)/);assert.match(leaflet,/brightness\(\.92\)/);assert.match(leaflet,/contrast\(1\.08\)/);assert.match(leaflet,/saturate\(\.12\)/);assert.match(leaflet,/FLYTALLY_MAP_TILES/);assert.doesNotMatch(leaflet,/tile\.openstreetmap\.org/)});test("v1.31.7 map remains supported by later releases",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,7))});
