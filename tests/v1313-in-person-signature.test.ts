import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import path from "node:path";import { releaseAtLeast } from "./release-version.ts";
const root=path.resolve(import.meta.dirname,".."),read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8"),credentialsSource=()=>read("app/(protected)/credentials/page.tsx")+read("app/(protected)/credentials/legacy-page.tsx")+read("app/(protected)/credentials/adaptive-overview.tsx");
test("in-person signature remains available for certified training flights",()=>{const entry=read("components/readonly-logbook-entry.tsx"),route=read("app/(protected)/flights/[id]/in-person-signature/page.tsx"),pad=read("components/in-person-signature-pad.tsx");assert.match(entry,/Instructor without FlyTally · Sign on this device/);assert.match(route,/In-person handwritten signature/);assert.match(pad,/onPointerDown/);assert.match(pad,/Apple Pencil/);assert.match(pad,/confirm_in_person/)});
test("in-person evidence stays bound to exact revision",()=>{const route=read("app/(protected)/flights/[id]/in-person-signature/page.tsx");assert.match(route,/recordRevision/);assert.match(route,/flightHash/);assert.match(route,/signerUserId:null/);assert.match(route,/signVerificationPayload/);assert.match(route,/identity was not independently authenticated by a FlyTally account/)});
test("LAPL refresher accepts signed instructor verification including in-person evidence",()=>{const licences=credentialsSource();assert.match(licences,/v\.verification_role='INSTRUCTOR' AND v\.status='signed'/);assert.doesNotMatch(licences,/v\.verification_role='INSTRUCTOR' AND v\.signer_user_id/)});
test("v1.31 in-person signature remains supported",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,3))});
