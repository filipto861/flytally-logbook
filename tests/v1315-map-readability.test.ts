import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import path from "node:path";const root=path.resolve(import.meta.dirname,".."),read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
test("map readability keeps filtered no-key OSM",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tilePane\.style\.filter/);assert.match(leaflet,/tile\.openstreetmap\.org/)});test("route contrast remains above basemap",()=>{const map=read("components/route-overview-map.tsx");assert.match(map,/baseOpacity=\.58\+\.36/);assert.match(map,/weight=2\+4\.5/)});test("v1.31 readability baseline remains supported",()=>{assert.match(JSON.parse(read("package.json")).version,/^1\.(31|32|33)\./)});
