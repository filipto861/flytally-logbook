import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";const read=(p:string)=>fs.readFileSync(new URL("../"+p,import.meta.url),"utf8"),releaseAtLeast=(v:string,a:number,b:number,c:number)=>{const[x,y,z]=v.split(".").map(Number);return x>a||x===a&&(y>b||y===b&&z>=c)};
test("map readability keeps the filtered FlyTally provider-backed basemap",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tilePane\.style\.filter/);assert.match(leaflet,/FLYTALLY_MAP_TILES/);assert.doesNotMatch(leaflet,/tile\.openstreetmap\.org/)});test("route contrast remains above basemap",()=>{const map=read("components/route-overview-map.tsx");assert.match(map,/baseOpacity=\.58\+\.36/);assert.match(map,/weight=2\+4\.5/)});test("v1.31 readability baseline remains supported",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,5))});
