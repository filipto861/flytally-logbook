import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import path from "node:path";import { releaseAtLeast } from "./release-version.ts";const root=path.resolve(import.meta.dirname,".."),read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
test("dark no-key OSM treatment remains",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tilePane\.style\.filter/);assert.match(leaflet,/tile\.openstreetmap\.org/)});test("route overlays remain independent from tile filtering",()=>{const map=read("components/route-overview-map.tsx");assert.match(map,/pane:"routeLines"/);assert.match(map,/pane:"airportMarkers"/)});test("v1.31 dark-map baseline remains supported",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,6))});
