import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";const read=(p:string)=>fs.readFileSync(new URL("../"+p,import.meta.url),"utf8"),releaseAtLeast=(v:string,a:number,b:number,c:number)=>{const[x,y,z]=v.split(".").map(Number);return x>a||x===a&&(y>b||y===b&&z>=c)};
test("dark proxy basemap treatment remains",()=>{const leaflet=read("components/leaflet-mobile.ts");assert.match(leaflet,/tilePane\.style\.filter/);assert.match(leaflet,/FLYTALLY_MAP_TILES/);assert.doesNotMatch(leaflet,/tile\.openstreetmap\.org/)});test("route overlays remain independent from tile filtering",()=>{const map=read("components/route-overview-map.tsx");assert.match(map,/pane:"routeLines"/);assert.match(map,/pane:"airportMarkers"/)});test("v1.31 dark-map baseline remains supported",()=>{assert.ok(releaseAtLeast(JSON.parse(read("package.json")).version,1,31,6))});
