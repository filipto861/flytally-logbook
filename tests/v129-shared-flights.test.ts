import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import path from "node:path";
const root=path.resolve(import.meta.dirname,".."),read=(file:string)=>fs.readFileSync(path.join(root,file),"utf8");
test("v1.29 participation schema remains the source/participant ownership model",()=>{const migration=read("lib/db-optimization.ts"),plan=read("lib/migration-plan.ts");assert.match(plan,/\{version:12,name:"shared flight participation"\}/);assert.match(migration,/CREATE TABLE IF NOT EXISTS flight_participations/);assert.match(migration,/UNIQUE\(participant_flight_id\)/)});
test("Safety Pilot invitations require a certified flight and an accepted connection",()=>{const actions=read("app/(protected)/flights/shared-actions.ts");assert.match(actions,/validCrewCombination/);assert.match(actions,/f\.certified_at IS NOT NULL/);assert.match(actions,/c\.status='accepted'/);assert.match(actions,/SAFETY PILOT/)});
test("participant acceptance rechecks the exact certification fingerprint",()=>{const actions=read("app/(protected)/flights/shared-actions.ts");assert.match(actions,/sf\.certification_hash=\$\{text\(row\.source_hash\)\}/);assert.match(actions,/COALESCE\(sf\.record_revision,1\)=\$\{Number\(row\.source_revision\)\}/);assert.match(actions,/pg_advisory_xact_lock/)});
test("shared entries copy certified facts into a separate owned draft",()=>{const actions=read("app/(protected)/flights/shared-actions.ts");assert.match(actions,/participant_flight_id=chosen\.id/);assert.match(actions,/INSERT INTO flight_tracks/);assert.match(actions,/source_track\.coordinates_json/);assert.match(actions,/INSERT INTO aircraft/);assert.doesNotMatch(actions,/\$\{text\(row\.note\)\}/)});
test("crew and instructor requests share one visible review workflow",()=>{const connections=read("app/(protected)/connections/page.tsx"),shared=read("app/(protected)/connections/shared/[id]/page.tsx"),sidebar=read("components/sidebar.tsx");assert.match(connections,/Flight requests/);assert.match(connections,/\/connections\/shared\//);assert.match(shared,/Sign &amp; add FI entry/);assert.match(shared,/Sign only/);assert.match(shared,/Add to my logbook/);assert.doesNotMatch(sidebar,/shared|participation|approval/i)});
