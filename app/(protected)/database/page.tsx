import { requireUser } from "@/lib/auth/require-user";
import { getDatabaseData } from "@/lib/data/database";
import { deleteRate,saveAircraft,saveAirport,saveRate,toggleAircraft,toggleAirport } from "./actions";
export const metadata={title:"Databáze | Letový zápisník"}; const t=(v:unknown)=>String(v??"");
export default async function DatabasePage(){const {userId}=await requireUser();const data=await getDatabaseData(userId);return <>
  <header className="page-header"><div><p className="eyebrow">SPRÁVA DAT</p><h1>Databáze</h1><p className="muted">Letadla, historické sazby a vlastní letiště</p></div></header><div className="data-tabs">
  <details className="panel" open><summary>Kontrola kvality dat</summary><div className="metric-grid compact-metrics"><article><span>Lety celkem</span><strong>{t(data.quality.total)}</strong></article><article><span>Možné duplicity</span><strong>{t(data.quality.duplicate_flights)}</strong></article><article><span>Bez ceny</span><strong>{t(data.quality.missing_price)}</strong></article><article><span>Neúplná trasa</span><strong>{t(data.quality.missing_route)}</strong></article><article><span>Neplatný BLOCK</span><strong>{t(data.quality.invalid_block_time)}</strong></article><article><span>Prázdné GPS</span><strong>{t(data.quality.empty_tracks)}</strong></article></div><p className="muted">Diagnostika nic sama nemění ani nemaže. Záznamy opravte v seznamu a detailu letů.</p></details>
  <details className="panel" open><summary>Letadla <b>{data.aircraft.length}</b></summary><form action={saveAircraft} className="inline-editor"><input name="registration" placeholder="Registrace" required/><input name="aircraft_type" placeholder="Typ letadla"/><input name="icao_type" placeholder="ICAO typ"/><select name="aircraft_class"><option value="">Třída</option><option>ULL</option><option>SEP</option><option>TMG</option></select><select name="evidence"><option value="">Evidence</option><option>ULL</option><option>EASA</option></select><input name="default_price_per_hour" type="number" step="0.01" placeholder="Cena/h"/><select name="default_role"><option>PIC</option><option>DUAL</option><option>SAFETY PILOT</option></select><select name="billing_basis"><option>BLOCK</option><option>AIR</option></select><input name="note" placeholder="Poznámka"/><button className="primary-button">Přidat / aktualizovat</button></form><div className="table-scroll"><table><thead><tr><th>Registrace</th><th>Typ</th><th>Evidence</th><th>Výchozí cena</th><th>Účtování</th><th>Stav</th></tr></thead><tbody>{data.aircraft.map(a=><tr key={t(a.id)}><td><strong>{t(a.registration)}</strong><small>{t(a.icao_type)}</small></td><td>{t(a.aircraft_type)}<small>{t(a.aircraft_class)}</small></td><td>{t(a.evidence)}</td><td>{Number(a.default_price_per_hour||0).toLocaleString('cs-CZ')} Kč/h</td><td>{t(a.billing_basis)}</td><td><form action={toggleAircraft}><input type="hidden" name="id" value={t(a.id)}/><button className={Number(a.active)?"status-on":"status-off"}>{Number(a.active)?"Aktivní":"Neaktivní"}</button></form></td></tr>)}</tbody></table></div></details>
  <details className="panel"><summary>Sazby <b>{data.rates.length}</b></summary><form action={saveRate} className="inline-editor"><input name="registration" placeholder="Registrace" required/><input name="aircraft_type" placeholder="Typ"/><label>Platí od<input name="valid_from" type="date"/></label><input name="price_per_hour" type="number" step="0.01" placeholder="Cena/h"/><input name="dry_price_per_hour" type="number" step="0.01" placeholder="Dry cena/h"/><input name="source" placeholder="Zdroj"/><button className="primary-button">Uložit sazbu</button></form><div className="table-scroll"><table><thead><tr><th>Registrace</th><th>Platí od</th><th>Cena</th><th>Dry</th><th>Zdroj</th><th></th></tr></thead><tbody>{data.rates.map(r=><tr key={t(r.id)}><td>{t(r.registration)}</td><td>{t(r.valid_from)||"—"}</td><td>{Number(r.price_per_hour||0).toLocaleString('cs-CZ')} Kč</td><td>{Number(r.dry_price_per_hour||0).toLocaleString('cs-CZ')} Kč</td><td>{t(r.source)}</td><td><form action={deleteRate}><input type="hidden" name="id" value={t(r.id)}/><button className="icon-danger">Smazat</button></form></td></tr>)}</tbody></table></div></details>
  <details className="panel"><summary>Vlastní letiště <b>{data.airports.length}</b></summary><form action={saveAirport} className="inline-editor"><input name="ident" placeholder="ICAO / kód" required/><input name="name" placeholder="Název"/><input name="municipality" placeholder="Město"/><input name="iso_country" placeholder="Země"/><input name="latitude_deg" type="number" step="any" placeholder="Latitude"/><input name="longitude_deg" type="number" step="any" placeholder="Longitude"/><button className="primary-button">Přidat / aktualizovat</button></form><div className="table-scroll"><table><thead><tr><th>Kód</th><th>Název</th><th>Město</th><th>Země</th><th>Souřadnice</th><th>Stav</th></tr></thead><tbody>{data.airports.map(a=><tr key={t(a.id)}><td><strong>{t(a.ident)}</strong></td><td>{t(a.name)}</td><td>{t(a.municipality)}</td><td>{t(a.iso_country)}</td><td>{t(a.latitude_deg)}, {t(a.longitude_deg)}</td><td><form action={toggleAirport}><input type="hidden" name="id" value={t(a.id)}/><button className={Number(a.active)?"status-on":"status-off"}>{Number(a.active)?"Aktivní":"Neaktivní"}</button></form></td></tr>)}</tbody></table></div></details>
  </div></>}
