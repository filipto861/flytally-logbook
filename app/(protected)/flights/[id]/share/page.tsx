import Link from "next/link";
import {notFound,redirect} from "next/navigation";
import {requireUser} from "@/lib/auth/require-user";
import {sql} from "@/lib/db";
import {getFlightTracks} from "@/lib/data/tracks";
import {createFlightShare,hasActiveFlightShare,revokeFlightShares} from "@/lib/flight-sharing";
import {FlightStoryCard} from "@/components/flight-story-card";

const mins=(a:unknown,b:unknown)=>{const p=(v:unknown)=>{const m=/^(\d{2}):(\d{2})$/.exec(String(v??""));return m?Number(m[1])*60+Number(m[2]):0},x=p(a),y=p(b);return x&&y?(y-x+1440)%1440:0};
export default async function ShareFlightPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{token?:string}>}){const {userId}=await requireUser(),id=Number((await params).id);if(!Number.isSafeInteger(id)||id<1)notFound();const rows=await sql`SELECT id,date,departure,arrival,registration,aircraft_type,aircraft_make,aircraft_model,off_block,on_block,COALESCE(landings_day,0)+COALESCE(landings_night,0) landings FROM flights WHERE id=${id} AND user_id=${userId} LIMIT 1` as Array<Record<string,unknown>>;const f=rows[0];if(!f)notFound();const [tracks,active]=await Promise.all([getFlightTracks(userId,id),hasActiveFlightShare(userId,id)]),token=(await searchParams).token?.trim()||"",aircraft=[f.aircraft_make,f.aircraft_model].map(x=>String(x??"").trim()).filter(Boolean).join(" ")||String(f.aircraft_type??"");async function create(form:FormData){"use server";const {userId:uid}=await requireUser();const t=await createFlightShare(uid,id,form.get("registration")==="on");redirect(`/flights/${id}/share?token=${encodeURIComponent(t)}`)}async function revoke(){"use server";const {userId:uid}=await requireUser();await revokeFlightShares(uid,id);redirect(`/flights/${id}/share`)}const distance=tracks.reduce((s,t)=>s+t.distanceKm,0),points=tracks.flatMap(t=>t.points);return <main className="page-shell"><div className="page-heading"><div><p className="eyebrow">SHARE FLIGHT</p><h1>{String(f.departure||"—")} → {String(f.arrival||"—")}</h1><p className="muted">Share only a deliberately limited public view. Logbook remarks, costs, signatures, licence data, certification and crew data are never included.</p></div><Link className="secondary-button" href={`/flights/${id}`}>Back to flight</Link></div><section className="panel"><p className="eyebrow">VIEW-ONLY LINK</p><h2>Public flight page</h2>{token?<><p>Your new link is ready. Copy it now; FlyTally stores only a one-way hash of the secret token.</p><div className="share-link-box"><code>{`/f/${token}`}</code></div><a className="primary-button" href={`/f/${token}`} target="_blank" rel="noreferrer">Open public view</a></>:<p className="muted">{active?"A public link is currently active. Create a new one to rotate the secret, or revoke sharing.":"No public link is active."}</p>}<form action={create} className="share-options"><label><input type="checkbox" name="registration"/> Include aircraft registration</label><button className="primary-button">{active?"Create new link":"Create public link"}</button></form>{active&&<form action={revoke}><button className="secondary-button">Revoke public link</button></form>}</section><section className="panel"><p className="eyebrow">SOCIAL</p><h2>Instagram Story</h2><p className="muted">1080×1920 card with the recorded flight path in perspective, public flight statistics and no regulatory/private logbook fields.</p><FlightStoryCard departure={String(f.departure??"")} arrival={String(f.arrival??"")} date={String(f.date??"")} aircraftType={aircraft} blockMinutes={mins(f.off_block,f.on_block)} landings={Number(f.landings??0)} distanceKm={distance} points={points}/></section></main>}
