"use client";
import { useActionState } from "react";
import { createInvitation,type InviteState } from "@/app/(protected)/admin/actions";

export function AuthInviteCreator(){const[state,action,pending]=useActionState<InviteState,FormData>(createInvitation,{});return <div className="invite-creator"><form action={action} className="inline-editor"><label>Tester email <span className="field-hint" aria-hidden="true">Required</span><input name="email" type="email" required placeholder="pilot@example.com"/></label><label>Valid for<select name="days" defaultValue="7"><option value="2">2 days</option><option value="7">7 days</option><option value="14">14 days</option></select></label><button className="primary-button" disabled={pending}>{pending?"Creating…":"Send invitation"}</button></form>{state.error?<p className="form-error" role="alert">{state.error}</p>:null}{state.url?<div className="invite-result"><strong>{state.sent?"Invitation sent":"Invitation ready"}</strong><p>{state.sent?"An email was sent to the invited pilot. Keep this private link as a fallback.":"Send this private link only to the invited pilot. It can be used once."}</p><input readOnly value={state.url} onFocus={event=>event.currentTarget.select()}/></div>:null}</div>}
