"use client";
import {useRef,useState} from "react";
import type {TrackPoint} from "@/lib/data/tracks";

type Props={departure:string;arrival:string;date:string;aircraftType:string;registration?:string;blockMinutes:number;landings:number;distanceKm:number;points:TrackPoint[]};
function projected(points:TrackPoint[]){if(!points.length)return[];const sample=points.filter((_,i)=>i===0||i===points.length-1||i%Math.max(1,Math.ceil(points.length/220))===0),xs=sample.map(p=>p.lon),ys=sample.map(p=>p.lat),as=sample.map(p=>Number(p.alt??0)),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),minA=Math.min(...as),maxA=Math.max(...as),rx=maxX-minX||1,ry=maxY-minY||1,ra=maxA-minA||1;return sample.map(p=>{const x=(p.lon-minX)/rx-.5,z=(p.lat-minY)/ry-.5,a=(Number(p.alt??0)-minA)/ra,d=z*.44;return{x:540+x*720-d*250,y:900+d*360-a*260,ground:900+d*360}})}
const duration=(m:number)=>m?`${Math.floor(m/60)}:${String(m%60).padStart(2,"0")}`:"—";
export function FlightStoryCard(props:Props){const svgRef=useRef<SVGSVGElement>(null),[busy,setBusy]=useState(false),p=projected(props.points),route=p.map(x=>`${x.x},${x.y}`).join(" "),shadow=p.map(x=>`${x.x},${x.ground}`).join(" ");async function save(){if(!svgRef.current)return;setBusy(true);try{const xml=new XMLSerializer().serializeToString(svgRef.current),blob=new Blob([xml],{type:"image/svg+xml;charset=utf-8"}),url=URL.createObjectURL(blob),img=new Image();await new Promise<void>((ok,bad)=>{img.onload=()=>ok();img.onerror=bad;img.src=url});const c=document.createElement("canvas");c.width=1080;c.height=1920;const ctx=c.getContext("2d");if(!ctx)return;ctx.drawImage(img,0,0,1080,1920);URL.revokeObjectURL(url);const png=await new Promise<Blob|null>(r=>c.toBlob(r,"image/png",.96));if(!png)return;const file=new File([png],`FlyTally-${props.departure}-${props.arrival}.png`,{type:"image/png"});if(navigator.share&&navigator.canShare?.({files:[file]})){await navigator.share({files:[file],title:`${props.departure} → ${props.arrival}`})}else{const a=document.createElement("a");a.href=URL.createObjectURL(png);a.download=file.name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}finally{setBusy(false)}}return <div className="story-card-tool"><div className="story-preview"><svg ref={svgRef} viewBox="0 0 1080 1920" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="bg" x1="0" y1="0" x2="1" y2="1"><stop stopColor="#061724"/><stop offset="1" stopColor="#0d3852"/></linearGradient><linearGradient id="line" x1="0" y1="0" x2="1" y2="0"><stop stopColor="#6ee7ff"/><stop offset="1" stopColor="#fff"/></linearGradient></defs><rect width="1080" height="1920" fill="url(#bg)"/><text x="72" y="105" fill="#8eb6ca" fontSize="30" fontFamily="Arial" fontWeight="700" letterSpacing="5">FLYTALLY · FLIGHT</text><text x="72" y="245" fill="white" fontSize="88" fontFamily="Arial" fontWeight="800">{props.departure||"—"} → {props.arrival||"—"}</text><text x="72" y="310" fill="#b7d1de" fontSize="34" fontFamily="Arial">{[props.aircraftType,props.registration,props.date].filter(Boolean).join("  ·  ")}</text><path d="M70 430 L1010 650 L1010 1120 L70 900 Z" fill="#0a2638" stroke="#25536a" strokeWidth="3"/><g opacity=".32" stroke="#4d8299" strokeWidth="2">{[0,1,2,3,4].map(i=><line key={`h${i}`} x1="70" y1={430+i*117} x2="1010" y2={650+i*117}/>)}{[0,1,2,3,4,5].map(i=><line key={`v${i}`} x1={70+i*188} y1={430+i*44} x2={70+i*188} y2={900+i*44}/>)}</g>{p.length>1&&<><polyline points={shadow} fill="none" stroke="#051018" strokeWidth="18" opacity=".55"/><polyline points={route} fill="none" stroke="url(#line)" strokeWidth="12" strokeLinecap="round" strokeLinejoin="round"/>{p.filter((_,i)=>i%Math.max(1,Math.ceil(p.length/14))===0).map((x,i)=><line key={i} x1={x.x} y1={x.y} x2={x.x} y2={x.ground} stroke="#54c7e8" opacity=".18" strokeWidth="3"/>)}<circle cx={p[0].x} cy={p[0].y} r="18" fill="#fff"/><circle cx={p.at(-1)!.x} cy={p.at(-1)!.y} r="18" fill="#6ee7ff"/></>}<text x="72" y="1255" fill="#8eb6ca" fontSize="28" fontFamily="Arial" fontWeight="700">FLIGHT TIME</text><text x="72" y="1340" fill="white" fontSize="70" fontFamily="Arial" fontWeight="800">{duration(props.blockMinutes)}</text><text x="405" y="1255" fill="#8eb6ca" fontSize="28" fontFamily="Arial" fontWeight="700">DISTANCE</text><text x="405" y="1340" fill="white" fontSize="70" fontFamily="Arial" fontWeight="800">{props.distanceKm?`${Math.round(props.distanceKm/1.852)} NM`:"—"}</text><text x="785" y="1255" fill="#8eb6ca" fontSize="28" fontFamily="Arial" fontWeight="700">LANDINGS</text><text x="785" y="1340" fill="white" fontSize="70" fontFamily="Arial" fontWeight="800">{props.landings}</text><line x1="72" y1="1510" x2="1008" y2="1510" stroke="#31586c"/><text x="72" y="1600" fill="white" fontSize="38" fontFamily="Arial" fontWeight="700">Logged with FlyTally</text><text x="72" y="1660" fill="#8eb6ca" fontSize="27" fontFamily="Arial">A private pilot logbook · shared by the pilot</text></svg></div><button type="button" className="primary-button" onClick={save} disabled={busy}>{busy?"Preparing…":"Share Story · 1080×1920"}</button></div>}
