# Story card v3

The social card keeps the flight itself as the hero: route, map, telemetry, then three summary figures. The telemetry chart uses independent labelled scales for recorded altitude in feet and derived GPS groundspeed in knots. No maximum-value badges are shown. Branding is intentionally limited to a small `fly-tally.com` signature at the bottom so the output remains useful first and discoverable second.
